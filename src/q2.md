# Question 2 — Python & Environment Setup

**Format:** Paste real output from your own machine

---

> There is no multiple choice here. We want to see what your setup looks like **right now**. Open the terminal listed for your OS, then run the commands below. **Copy the commands AND their real output** into the code block. Do not invent or paste output from anywhere else. We are checking *your* machine.

| Your OS | Open this |
|---------|-----------|
| macOS | Terminal.app or iTerm2 |
| Linux | your distribution's Terminal (bash or zsh) |
| Windows | WSL/Ubuntu Terminal (preferred), or PowerShell/Command Prompt |
>
> **You do not need to install anything just for this question.** If a tool isn't installed yet, that is completely fine. Tell us honestly and say what you would do next. We are finding your starting point, not failing you.

> **Windows users**: If you would like to install WSL on your machine, please read the [**`SETUP-WINDOWS.md`**](./../SETUP-WINDOWS.md) guide here. 

**(a)** Show your Python version. Run **one** of:

```
python --version
python3 --version
```

> Paste the command and its real output even if it fails. If Python isn't installed, the output will say something like command not found or 'python' is not recognized — that error message is your answer, so paste it. Then add one sentence saying what you would install to get Python.

<sub><i>
(base) C:\Users\jolene_chiam>python --version
Python 3.12.7

(base) C:\Users\jolene_chiam>python3 --version
Python was not found; run without arguments to install from the Microsoft Store, or disable this shortcut from Settings > Apps > Advanced app settings > App execution aliases.
</i></sub>

**(b)** Show your installed conda environments:

```
conda env list
```

> Run this even if you haven't installed conda — and paste whatever comes back, error included. Then add one sentence saying what you would install. Honesty still scores here: a real error message from your machine plus an honest sentence is a complete answer. We are finding your starting point, not failing you.


**Paste your terminal session below (commands + output):**


**Answer:**

```text
<sub><i>
(base) C:\Users\jolene_chiam>conda env list

# conda environments:
#
                       C:\Users\jolene_chiam\AppData\Local\Programs\Orange
base                 * C:\Users\jolene_chiam\AppData\Local\anaconda3
credit-risk            C:\Users\jolene_chiam\AppData\Local\anaconda3\envs\credit-risk
kiro_env               C:\Users\jolene_chiam\AppData\Local\anaconda3\envs\kiro_env
</i></sub>
```

---

**(c)** In one sentence: what did the Python version number tell you, and how would you check whether a package (e.g. `pandas`) is installed in your current environment?


**Answer:**
<sub<i>
It tells me of the Python version that is installed (3.12.7). So if I need to use a older or newer version, I can install accordingly.

ChatGPT says to use <b>conda list pandas</b> I tested and it worked (version 2.2.2).
</i></sub>

---
